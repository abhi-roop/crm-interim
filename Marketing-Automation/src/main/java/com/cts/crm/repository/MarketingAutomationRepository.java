package com.cts.crm.repository;

import com.cts.crm.model.MarketingAutomationModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MarketingAutomationRepository extends JpaRepository<MarketingAutomationModel, Long> {
}
